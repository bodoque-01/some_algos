[project]
name = "rotating-cube"
version = "0.1.0"
description = "A 3D rotating cube visualization using pygame"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.21.0",
    "pygame>=2.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_backend"
