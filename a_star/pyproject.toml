[project]
name = "a-star"
version = "0.1.0"
description = "A* pathfinding algorithm visualization using pygame"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.0.0",
    "numpy>=1.21.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_backend"
