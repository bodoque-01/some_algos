[project]
name = "a-star"
version = "0.1.0"
description = "A* pathfinding algorithm visualization using pygame"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.0.0",
    "numpy>=1.21.0",
]

[tool.uv]
package = false
