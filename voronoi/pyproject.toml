[project]
name = "voronoi"
version = "0.1.0"
description = "Grid-based visualization (Voronoi / pathfinding experiments)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0.0",
    "pygame>=2.5.0",
]

[tool.uv]
package = false
